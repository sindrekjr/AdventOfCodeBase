using AdventOfCode.Services;
using AdventOfCode.Solutions;

var solutions = new SolutionCollector();

foreach (var solution in solutions)
{
    Console.WriteLine();
    Console.WriteLine(solution.ToString());
}
